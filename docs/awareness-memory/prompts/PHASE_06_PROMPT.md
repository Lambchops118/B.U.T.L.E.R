# Launcher — Phase 06 Long-Term Memory

Execute **Phase 6 only: validated semantic/episodic memory, provenance, contradiction/supersession, configured local embeddings, and bounded hybrid retrieval**. Keep state, telemetry, conversations, and memory distinct.

## Required reading and entry check

Read root `AGENTS.md`, status, `docs/awareness-memory/phases/PHASE_06_LONG_TERM_MEMORY.md`, discovery/decisions, and only its named schema/failure/security/test references. Verify Phase 5 completion/review, understood evidence and conversation models, approved local embedding/vector choice and privacy policy, and Phase 6 authorization. Inspect relevant existing memory/conversation/Ollama code before editing. Avoid unrelated phases, full original, and large/generated files.

## Permitted changes

Implement focused memory candidate/manager/provenance/relationship/contradiction/embedding/retrieval work, strict proposal/tool schemas, local configuration, migrations/indexes, tests, and docs.

## Prohibited changes

No unrestricted model memory writes, raw telemetry/heartbeat/audio/binary or indiscriminate message embedding, fixed-interval episode spam, cloud services, current-state replacement, actions, broad Phase 8 consolidation/retention, placeholders, unrelated refactors, unauthorized teams, or Phase 7 work.

## Execution discipline

Start with `git status`, preserve unrelated changes, and inspect existing conversation/evidence/model paths with targeted searches. Treat a model proposal as untrusted input and require durable evidence before acceptance. Preserve old facts and validity history; never simplify contradiction into overwrite. Keep embedding outside transactions through bounded idempotent outbox work, record exact model/dimension/version/hash, and make degraded search capabilities visible. Build corpus inclusion and exclusion as testable policy, not convention. Keep hybrid results filtered, bounded, explainable, and subordinate to exact structured retrieval for exact questions. If sensitivity, user confirmation, deletion, or embedding model policy is unresolved, stop on that decision. Before handoff, audit for accidental transcript/telemetry embedding, cloud calls, action work, and unsupported success claims.

At checkpoints, compare the diff with the phase requirement IDs and acceptance criteria. Keep a compact record of commands and decisions so status and handoff are complete for a fresh session. When blocked, exhaust safe read-only evidence, identify the exact missing input or permission, and do not broaden scope.

## Deliverables and tests

Deliver distinct memory types/statuses/validity/sensitivity, deterministic unambiguous writes, strict model proposals validated against evidence, recorded accept/reject, duplicates/merge, explicit conflict, history-preserving supersession, local embedding metadata/outbox/retry, selected corpus, and bounded filtered hybrid scoring with provenance/component scores.

Test explicit preference acceptance, unsupported proposal rejection, evidence links, overflow episode, changed preference supersession, inconclusive conflict, corpus exclusions, model/dimension/version, embedding outage with exact/full-text continuity and retry, bounded hybrid filters/scores, conversation separation, sensitivity/deletion authorization, and migrations. Report commands/results truthfully.

Update memory/write/evidence/embedding/retrieval/privacy/outage docs, traceability, status, decisions/questions, and handoff.

## Final response

Report files/migrations; types/write/evidence policies; embedding/retrieval configuration; tests including rejected/outage cases; privacy/security/deployment effects; failures/limitations; repository state; next proposed task; explicit stop.

Stop after Phase 6. Do not implement actions or begin Phase 7.
