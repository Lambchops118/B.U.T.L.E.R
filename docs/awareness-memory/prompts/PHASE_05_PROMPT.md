# Launcher — Phase 05 Situation, Context, and Read Tools

Execute **Phase 5 only: compact situation, deterministic context selection, hard token budgets/audit, narrow structured read tools, and existing Ollama/Qwen integration**.

## Required reading and entry check

Read root `AGENTS.md`, status, `docs/awareness-memory/phases/PHASE_05_CONTEXT_AND_RETRIEVAL.md`, discovery/decisions, and only its listed context/schema/security/test references. Verify Phase 4 completion/review, stable qualified reads and alerts, documented existing LLM/tool/tokenizer behavior, and Phase 5 authorization. Inspect current prompt/tool code before editing. Do not load later phases, all prompts, full original specification, or unrelated/generated/large files.

## Permitted changes

Implement focused situation/context/budget/audit, strict read schemas/router/tools, existing-client integration, narrowly required audit migration, tests, and documentation.

## Prohibited changes

No long-term memory/embeddings/writes, arbitrary SQL/file/shell/MQTT, action tools, model hot-path control, LLM-client rewrite, context dumping, unbounded tool rounds/results, placeholders, unrelated refactors, unauthorized agent teams, or Phase 6 work.

## Execution discipline

Start with `git status`, preserve unrelated changes, and inspect existing prompt/tool code through focused searches. Build on typed read services instead of embedding database queries in prompt assembly. Treat every context item as budgeted evidence: preserve its identity, selection reason, provenance, temporal qualification, and truncation result. Test token accounting using the real tokenizer when available and document any conservative estimator. Enforce limits at validation and repository layers so a model cannot negotiate them away. A tool error or stale source becomes a truthful bounded result, never invented context. If the existing client lacks a required safe integration point, document the smallest additive change or blocker rather than rewriting it. Before handoff, review the diff for memory/action leakage and verify actual context/tool outputs stay bounded.

At checkpoints, compare the diff with the phase requirement IDs and acceptance criteria. Keep a compact record of commands and decisions so status and handoff are complete for a fresh session. When blocked, exhaust safe read-only evidence, identify the exact missing input or permission, and do not broaden scope.

## Deliverables and tests

Deliver a structured relevance-selected situation, canonical priority order, separate enforced budgets and actual/conservative tokenizer, low-priority-first truncation with critical-alert preservation, selection audit, explicit temporal rendering, and minimum state/room/entity/alert/attention/event/telemetry/health/provenance/capability tools. Validate auth, inputs, time/point/result/token/round bounds, log failures, and return clear errors.

Test unrelated-state exclusion, token/audit correctness, context overflow, temporal qualification, stale source truthfulness, current pump routing to state, last run to events, average current to aggregate, trust to health/provenance, invalid/unbounded input, stubbed LLM integration, and Ollama outage. Record exact commands and truthful results.

Update context/tool/routing/limit/error/model-integration docs, traceability, status, decisions/questions, and handoff.

## Final response

Report files/migrations; situation/relevance/budgets; tools/routes/limits; test/token evidence; failures/limitations; security and model-integration effects; repository state; next proposed task; explicit stop.

Stop after Phase 5. Do not implement long-term memory or begin Phase 6.
