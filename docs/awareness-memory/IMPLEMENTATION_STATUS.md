# Implementation Status

This file reports implementation state, not documentation availability.

| Field | Current value |
|---|---|
| Current phase | Phase 8 — Retention, Security, and Hardening — **complete**. All phases 0-8 implemented. |
| Phase state | Subsystem implementation complete on `memory_system_3_07152026` (owner authorized Phase 8 with local-backup defaults, 2026-07-16) |
| Last completed phase | Phase 8 (2026-07-16) |
| Current bounded task | Quad-pump firmware rewrite and bounded awareness integration — **software complete, physically unverified** (2026-07-26, owner-authorized via `Peripherals/quad_pump/plan.md`). Replaced the blocking legacy firmware with non-blocking, deadline-enforcing firmware (unique MQTT identity, canonical envelopes, command IDs/acks, persistent dedup, bounded reconnect, watchdog); registered the `quad_pump_canonical` source; added `run_pump`/`stop_pump` and per-channel cooldown scope. Fuse monitoring is deliberately absent (ADR-019). Firmware was **not flashed** and no bench or physical check was run. |
| Completed items | Phases 0-7 (see git history and `talos/awareness/README.md`); Phase 8: retention service (dry-run plan, bounded resumable batched deletion, aggregate-before-delete via cagg refresh, open-alert/evidence/active-memory protections), memory consolidation (incident summaries with derived_from links, weak-inference decay, user-evidence exemption), artifact store (generated rooted paths, SHA-256, table `artifacts`, migration `3337c328523b`), local backups (pg_dump in-container + config snapshot + 14-day pruning; **restore tested live: 27/27 tables**), write-auth on all mutating endpoints (actions fail-closed; others bearer-gated when `TALOS_AWARENESS_API_TOKEN` set), `/metrics` (counters/backlog/disk/last-backup), benchmark utility (**118 ev/s, p50 7.5 ms, p95 14.8 ms, 0 drops**), broker hardening plan (`BROKER_HARDENING_PLAN.md`, owner-executed), CLI: `retention`/`consolidate`/`backup [--verify]` |
| Active work | None |
| Blocked items | Owner-executed items only: broker auth/ACL/TLS on the Pi (OQ-B plan delivered); quad-pump bench + physical acceptance and Wi-Fi credential rotation (this session); fuse-sensing hardware decision (OQ-D); channel-to-pot verification (OQ-E); live-model retrieval scenario runs with real Qwen |
| Decisions made | ADR-001..021 in `DECISIONS.md`; ADR-017 selects local `mb-core-v1:latest` for streamed inference with remote STT/LLM fallback opt-in; ADR-018..021 record the quad-pump hardware/safety policy, the no-fuse-monitoring decision, the staged rollout, and the retained at-most-once retry policy; OQ-013 resolved 2026-07-16 (local nightly backups, 14-day retention, no encryption, tested restore) |
| Assumptions confirmed | `DISCOVERY.md` §12/§15; open-question table in `OPEN_QUESTIONS.md`; quad-pump pin numbering, relay direction/polarity, concurrency and run-time limits, pot mapping, and fuse policy confirmed by the owner in chat 2026-07-26 |
| Open questions | OQ-B (broker config verification + hardening execution), OQ-C (fan Pico firmware only — resolved for the quad pump), OQ-D (quad-pump fuse ADC path), OQ-E (channel-to-pot wiring verification), OQ-F (command-topic dead letters) |
| Tests last run | 2026-07-26 quad pump: 65 host-side firmware tests passed (`tests/test_quad_pump_firmware.py`) covering safe boot, channel mapping, polarity, command schema failures, duration bounds, deadlines, stop, concurrency limit, fuse debounce/hysteresis and fault handling, envelope/boot-id/sequence generation, in-memory and post-restart deduplication, ledger corruption recovery, queue bounds, backoff, and legacy compatibility. `compileall` passed for all changed Python. `actions.toml` was structurally validated against the registry's rules. **All awareness tests skipped, not run** — awareness dependencies are not installed on this machine (Python 3.9 only, no `.venv-awareness`), so the new awareness assertions are unverified by execution. No firmware was flashed and no bench, meter, fuse, power-loss, network-interruption, or physical acceptance check was performed. Earlier results remain in their session handoffs. |
| Known failures | None |
| Files recently modified | Quad pump: `Peripherals/quad_pump/main.py` plus new `qp_config.py`, `qp_hardware.py`, `qp_protocol.py`, `qp_ledger.py`, `qp_controller.py`, `qp_net.py`, `qp_secrets_example.py`; `tests/test_quad_pump_firmware.py`; `talos/awareness/actions/{actions.toml,registry.py,service.py}`; `talos/awareness/registry/bootstrap.py`; `talos/mcp_servers/providers/awareness.py`; `talos/awareness/README.md`; `.gitignore`; awareness tests; `DECISIONS.md`; `OPEN_QUESTIONS.md`; this status; `SESSION_HANDOFF_2026-07-26_QUAD_PUMP_FIRMWARE.md`. Pre-existing local changes were preserved. |
| Next permitted task | Owner-executed quad-pump bench validation on the new board (meter MCU inputs, dummy-load the relays, confirm outputs stay off through boot/reset/exception/network loss/watchdog, verify channel-to-relay mapping per OQ-E, duplicate-delivery and reset-at-each-phase tests), then physical acceptance. Switching `water_plants` to the canonical command, moving the pump actions to `device_key` idempotency, adding fuse alerting, other peripherals' firmware, broker hardening, and any new awareness phase each require a separate explicit task. |
| Required reading | `talos/awareness/README.md` (complete operational reference), `Peripherals/quad_pump/plan.md`, `DISCOVERY.md`, `BROKER_HARDENING_PLAN.md`, latest session handoff |
| Explicit stop condition | Stop after the quad-pump firmware, its bounded awareness integration, host tests, and documentation. Do not start firmware work for another peripheral, broker-wide hardening beyond the approved quad-pump deployment step, or any new awareness phase without separate owner authorization. |
| Documentation follow-up (2026-07-16) | Added `like_im_a_child_or_golden_retriever.md`, a plain-language intern quick start covering immediate operation, TALOS integration, maintenance, code paths, tests, safety invariants, limitations, and troubleshooting. Linked it from this documentation index. Validation: 96 awareness unit tests and 3 main-agent home-action tests pass; CLI help, relative-link targets, and `git diff --check` pass. Runtime, schemas, migrations, decisions, and open questions are unchanged. |

Do not infer implementation progress from the presence of specification or launcher files. Session handoffs live in dated files derived from `SESSION_HANDOFF_TEMPLATE.md` (latest: `SESSION_HANDOFF_2026-07-26_QUAD_PUMP_FIRMWARE.md`).
