# Implementation Status

This file reports implementation state, not documentation availability.

| Field | Current value |
|---|---|
| Current phase | Phase 8 — Retention, Security, and Hardening — **complete**. All phases 0-8 implemented. |
| Phase state | Subsystem implementation complete on `memory_system_3_07152026` (owner authorized Phase 8 with local-backup defaults, 2026-07-16) |
| Last completed phase | Phase 8 (2026-07-16) |
| Current bounded task | Quad-pump GPIO mapping hotfix — **software complete; corrected `qp_config.py` must be copied to the Pico and physically verified** (2026-07-26). Live action audits proved the backend and board firmware completed eight-second runs, but no relay clicked. The owner's known-working direct MicroPython code then proved the physical relay inputs are active-high GP9-GP12, not GP0-GP3. Channel 1-4 now maps to GP9-GP12; the original table's unavailable fuse inputs are recorded as GP1/GP2/GP4/GP5. ADR-022 supersedes the GPIO portion of ADR-018. The voice action truthfulness fixes remain complete. |
| Completed items | Phases 0-7 (see git history and `talos/awareness/README.md`); Phase 8: retention service (dry-run plan, bounded resumable batched deletion, aggregate-before-delete via cagg refresh, open-alert/evidence/active-memory protections), memory consolidation (incident summaries with derived_from links, weak-inference decay, user-evidence exemption), artifact store (generated rooted paths, SHA-256, table `artifacts`, migration `3337c328523b`), local backups (pg_dump in-container + config snapshot + 14-day pruning; **restore tested live: 27/27 tables**), write-auth on all mutating endpoints (actions fail-closed; others bearer-gated when `TALOS_AWARENESS_API_TOKEN` set), `/metrics` (counters/backlog/disk/last-backup), benchmark utility (**118 ev/s, p50 7.5 ms, p95 14.8 ms, 0 drops**), broker hardening plan (`BROKER_HARDENING_PLAN.md`, owner-executed), CLI: `retention`/`consolidate`/`backup [--verify]` |
| Active work | None |
| Blocked items | Owner-executed items only: restart TALOS so main/voice load the physical-action routing fix; broker auth/ACL/TLS on the Pi (OQ-B plan delivered); quad-pump bench + physical acceptance and Wi-Fi credential rotation; fuse-sensing hardware decision (OQ-D); channel-to-pot verification (OQ-E); live-model retrieval scenario runs with real Qwen |
| Decisions made | ADR-001..021 in `DECISIONS.md`; ADR-017 selects local `mb-core-v1:latest` for streamed inference with remote STT/LLM fallback opt-in; ADR-018..021 record the quad-pump hardware/safety policy, the no-fuse-monitoring decision, the staged rollout, and the retained at-most-once retry policy; OQ-013 resolved 2026-07-16 (local nightly backups, 14-day retention, no encryption, tested restore) |
| Assumptions confirmed | `DISCOVERY.md` §12/§15; open-question table in `OPEN_QUESTIONS.md`; quad-pump pin numbering, relay direction/polarity, concurrency and run-time limits, pot mapping, and fuse policy confirmed by the owner in chat 2026-07-26 |
| Open questions | OQ-B (broker config verification + hardening execution), OQ-C (fan Pico firmware only — resolved for the quad pump), OQ-D (quad-pump fuse ADC path), OQ-E (channel-to-pot wiring verification), OQ-F (command-topic dead letters) |
| Tests last run | 2026-07-26 voice action hotfix: 46 focused unit tests passed (`test_run_command_stream`, leaked-tool recovery, prompting, current-time context, tool scoping, thinking); `py_compile` passed for both changed Python files. Because WSL-to-Windows process interop failed after the host reboot and Linux lacks the `openai` package, the suite used the Windows venv's pure-Python packages with an import-only `openai` stub; the tested streamed path uses the injected fake backend and does not call that module. A read-only live MQTT trace received recurring quad-pump heartbeats from one stable boot ID. Earlier Windows MQTT and 65-test firmware results remain recorded in their handoffs. |
| Known failures | The Pico still has the incorrect GP0-GP3 mapping until the corrected `Peripherals/quad_pump/qp_config.py` is copied onto its root and it restarts. Main/voice also need a restart if the shortened no-UUID action wording has not yet been loaded. Physical relay/channel acceptance remains pending. |
| Files recently modified | GPIO mapping hotfix: `Peripherals/quad_pump/{qp_config.py,qp_hardware.py,plan.md}`, `tests/test_quad_pump_firmware.py`, `DECISIONS.md`, `OPEN_QUESTIONS.md`, this status, and `SESSION_HANDOFF_2026-07-26_QUAD_PUMP_GPIO_MAPPING.md`. Voice action files from the immediately prior hotfix remain listed in their handoff. Pre-existing local changes were preserved. |
| Next permitted task | Copy corrected `qp_config.py` to the Pico root, restart it, confirm a new boot heartbeat, then run each channel once under observation and verify GP9-GP12 relay clicks/channel assignment. Continue with the plan's bench/physical acceptance before unattended watering. Switching `water_plants` to the canonical command, moving pump actions to `device_key` idempotency, adding fuse alerting, other peripherals' firmware, broker hardening, and any new awareness phase each require a separate explicit task. |
| Required reading | `talos/awareness/README.md`, `Peripherals/quad_pump/plan.md`, `DISCOVERY.md`, `BROKER_HARDENING_PLAN.md`, `SESSION_HANDOFF_2026-07-26_QUAD_PUMP_GPIO_MAPPING.md`, `SESSION_HANDOFF_2026-07-26_VOICE_ACTION_DISPATCH.md` |
| Explicit stop condition | Stop after the quad-pump firmware, its bounded awareness integration, host tests, and documentation. Do not start firmware work for another peripheral, broker-wide hardening beyond the approved quad-pump deployment step, or any new awareness phase without separate owner authorization. |
| Documentation follow-up (2026-07-16) | Added `like_im_a_child_or_golden_retriever.md`, a plain-language intern quick start covering immediate operation, TALOS integration, maintenance, code paths, tests, safety invariants, limitations, and troubleshooting. Linked it from this documentation index. Validation: 96 awareness unit tests and 3 main-agent home-action tests pass; CLI help, relative-link targets, and `git diff --check` pass. Runtime, schemas, migrations, decisions, and open questions are unchanged. |

Do not infer implementation progress from the presence of specification or launcher files. Session handoffs live in dated files derived from `SESSION_HANDOFF_TEMPLATE.md` (latest: `SESSION_HANDOFF_2026-07-26_QUAD_PUMP_GPIO_MAPPING.md`).
