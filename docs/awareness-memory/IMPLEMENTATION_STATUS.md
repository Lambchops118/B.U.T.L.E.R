# Implementation Status

This file reports implementation state, not documentation availability.

| Field | Current value |
|---|---|
| Current phase | Phase 8 — Retention, Security, and Hardening — **complete**. All phases 0-8 implemented. |
| Phase state | Subsystem implementation complete on `memory_system_3_07152026` (owner authorized Phase 8 with local-backup defaults, 2026-07-16) |
| Last completed phase | Phase 8 (2026-07-16) |
| Current bounded task | Barge-in voice interruption design review — **complete; runtime redesign not started** (2026-07-26). The current RMS-plus-ASR front end cannot reliably distinguish human/speaker double-talk: it structurally misses normal speech, falsely ducks on energy, and can accept non-speech Whisper hallucinations. An AEC-first replacement, interim containment, phased implementation, and acceptance corpus are documented in `docs/voice/BARGE_IN_REDESIGN_PLAN.md`; exact Windows endpoint/WebRTC backend selection is OQ-H. The earlier quad-pump GPIO software hotfix remains complete but still requires Pico deployment and physical verification. |
| Completed items | Phases 0-7 (see git history and `talos/awareness/README.md`); Phase 8: retention service (dry-run plan, bounded resumable batched deletion, aggregate-before-delete via cagg refresh, open-alert/evidence/active-memory protections), memory consolidation (incident summaries with derived_from links, weak-inference decay, user-evidence exemption), artifact store (generated rooted paths, SHA-256, table `artifacts`, migration `3337c328523b`), local backups (pg_dump in-container + config snapshot + 14-day pruning; **restore tested live: 27/27 tables**), write-auth on all mutating endpoints (actions fail-closed; others bearer-gated when `TALOS_AWARENESS_API_TOKEN` set), `/metrics` (counters/backlog/disk/last-backup), benchmark utility (**118 ev/s, p50 7.5 ms, p95 14.8 ms, 0 drops**), broker hardening plan (`BROKER_HARDENING_PLAN.md`, owner-executed), CLI: `retention`/`consolidate`/`backup [--verify]` |
| Active work | None |
| Blocked items | Owner authorization/evidence for the barge-in redesign: safe interim configuration choice, permission for an opt-in local synchronized audio corpus, exact Windows audio endpoints/build/driver AEC capability, and the OQ-H backend spike. Owner-executed items from earlier work also remain: restart TALOS so main/voice load the physical-action routing fix; broker auth/ACL/TLS on the Pi (OQ-B plan delivered); quad-pump bench + physical acceptance and Wi-Fi credential rotation; fuse-sensing hardware decision (OQ-D); channel-to-pot verification (OQ-E); live-model retrieval scenario runs with real Qwen. |
| Decisions made | ADR-001..021 in `DECISIONS.md`; ADR-017 selects local `mb-core-v1:latest` for streamed inference with remote STT/LLM fallback opt-in; ADR-018..021 record the quad-pump hardware/safety policy, the no-fuse-monitoring decision, the staged rollout, and the retained at-most-once retry policy; OQ-013 resolved 2026-07-16 (local nightly backups, 14-day retention, no encryption, tested restore) |
| Assumptions confirmed | `DISCOVERY.md` §12/§15; open-question table in `OPEN_QUESTIONS.md`; quad-pump pin numbering, relay direction/polarity, concurrency and run-time limits, pot mapping, and fuse policy confirmed by the owner in chat 2026-07-26 |
| Open questions | OQ-B (broker config verification + hardening execution), OQ-C (fan Pico firmware only — resolved for the quad pump), OQ-D (quad-pump fuse ADC path), OQ-E (channel-to-pot wiring verification), OQ-F (command-topic dead letters), OQ-H (deployed Windows AEC path and endpoint evidence) |
| Tests last run | 2026-07-26 barge-in review: 43 dependency-light tests passed (`tests.test_barge_in`, `tests.test_streaming_speaker`). The attempted Windows-venv four-suite run did not start because WSL-to-Windows interop failed with `UtilBindVsockAnyPort: socket failed 1`; no live audio/AEC/model test was run. Earlier voice-action, Windows MQTT, and firmware results remain recorded in their handoffs. |
| Known failures | Barge-in remains unreliable and tracked `settings.env` currently enables it; safe interim posture is `TALOS_BARGE_IN=0`, while wake-word-required mode is only partial containment. The Pico also still has the incorrect GP0-GP3 mapping until corrected `Peripherals/quad_pump/qp_config.py` is copied to its root and it restarts. Main/voice need a restart if shortened no-UUID action wording has not loaded. Physical relay/channel acceptance remains pending. |
| Files recently modified | Barge-in review: `docs/voice/BARGE_IN_REDESIGN_PLAN.md`, `README.md`, `OPEN_QUESTIONS.md`, this status, and `SESSION_HANDOFF_2026-07-26_BARGE_IN_REVIEW.md`. Earlier GPIO and voice-action files remain listed in their handoffs. Pre-existing local changes were preserved. |
| Next permitted task | With explicit owner authorization, execute barge-in redesign Phase A and then the Windows AEC feasibility spike in `docs/voice/BARGE_IN_REDESIGN_PLAN.md`; do not install/refactor/record audio before that authorization. Separately, copy corrected `qp_config.py` to the Pico, restart it, confirm a new boot heartbeat, and complete observed GP9-GP12 relay/channel acceptance. Other firmware, broker hardening, and any new awareness phase remain separately gated. |
| Required reading | `docs/voice/BARGE_IN_REDESIGN_PLAN.md`, `talos/voice/streaming/barge_in.py`, `talos/voice/agent.py`, `tests/test_barge_in.py`, `ARCHITECTURAL_INVARIANTS.md`; for pending pump work also `Peripherals/quad_pump/plan.md` and `SESSION_HANDOFF_2026-07-26_QUAD_PUMP_GPIO_MAPPING.md`. |
| Explicit stop condition | Stop after the barge-in design review and documentation. Do not install an AEC dependency, refactor the audio path, record room audio, run live speaker tests, begin other peripheral firmware, or start any new awareness phase without separate owner authorization. |
| Documentation follow-up (2026-07-16) | Added `like_im_a_child_or_golden_retriever.md`, a plain-language intern quick start covering immediate operation, TALOS integration, maintenance, code paths, tests, safety invariants, limitations, and troubleshooting. Linked it from this documentation index. Validation: 96 awareness unit tests and 3 main-agent home-action tests pass; CLI help, relative-link targets, and `git diff --check` pass. Runtime, schemas, migrations, decisions, and open questions are unchanged. |

Do not infer implementation progress from the presence of specification or launcher files. Session handoffs live in dated files derived from `SESSION_HANDOFF_TEMPLATE.md` (latest: `SESSION_HANDOFF_2026-07-26_BARGE_IN_REVIEW.md`).
