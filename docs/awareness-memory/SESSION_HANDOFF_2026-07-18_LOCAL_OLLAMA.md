# Session Handoff — 2026-07-18 — Local Ollama Inference

```text
Session goal: Replace the streamed home-automation agent's hosted OpenAI Chat Completions target with the owner's locally installed custom Ollama model while keeping STT and inference local-first.
Current phase: Phases 0-8 remain complete; this was an owner-authorized post-completion bounded runtime configuration and integration task.
Bounded task completed: Identified the installed model as mb-core-v1:latest; configured the ignored live .env for Ollama on loopback; changed the streamed runtime to use the existing backend factory; made Ollama the factory default with a loopback endpoint; removed the streamed lane's OPENAI_API_KEY fallback; made remote STT and legacy remote LLM failover explicit opt-ins; documented and tested the profile. Correction follow-up: recovered the exact latest pre-cutover live `.env` from VS Code Local History and merged the eight new settings, restoring all prior machine-specific values without displaying them.
Files added: Ignored local .env; docs/awareness-memory/SESSION_HANDOFF_2026-07-18_LOCAL_OLLAMA.md.
Files modified: .env.example, README.md, talos/agent/runtime.py, talos/voice/agent.py, talos/voice/backends/factory.py, tests/test_llm_openai_compat.py, docs/awareness-memory/IMPLEMENTATION_STATUS.md.
Migrations added: None.
Decisions made: ADR-017 records TALOS_LLM_BACKEND=ollama, TALOS_LLM_BASE_URL=http://127.0.0.1:11434/v1, and TALOS_LLM_MODEL=mb-core-v1:latest for the default streamed lane. Remote STT and voice LLM fallback default off to preserve the local-only boundary.
Assumptions confirmed or changed: Ollama is installed on the Windows host and mb-core-v1:latest is available. Its OpenAI-compatible Chat Completions endpoint works on loopback and supports the existing client seam. The prior streamed runtime bypassed the shared factory and could fall back to hosted OpenAI configuration.
Tests run: Main venv unittest focused suite covering LLM compatibility/factory, faster-whisper backend, memory, streamed command, text server/client, runtime recovery, prompting, voice routing, and awareness client/provider; Python py_compile on changed Python/test files; direct loopback /v1/chat/completions smoke; application-level get_llm_backend smoke; corrective key-set comparison of recovered `.env` against the latest pre-cutover history snapshot and `.env.example` against the repository version.
Tests passed: 55/55 focused unit tests; py_compile; direct Ollama response LOCAL_OLLAMA_OK; TALOS factory response TALOS_FACTORY_OK using mb-core-v1:latest; 25/25 prior live `.env` keys restored plus 8 new keys, with 0 missing and 0 duplicates; 0 original `.env.example` assignment keys missing (34 original, 39 current).
Tests failed: Initial sandboxed attempts to execute Windows ollama/Python binaries could not bind WSL vsock; the same commands passed when run with approved host access. First intentionally token-limited Ollama probe reached the model but ended before content at 32 completion tokens; the 256-token rerun passed.
Commands not run: Full repository suite; live microphone/faster-whisper audio capture; full TALOS voice command because it could expose physical home-automation tools; any physical action.
Known limitations: The default streamed voice LLM and STT lanes are local. The legacy non-streaming run_command path still uses hosted OpenAI Responses when explicitly selected, and speech synthesis still uses AWS Polly, so those separately bounded migrations are required before claiming the entire voice stack has no network dependency. Weather and other explicitly hosted integrations also remain network-dependent.
Security implications: The local LLM target is loopback and no API key is used. Audio stays local by default; remote transcription and legacy remote LLM failover require explicit opt-in. Existing physical-action schemas, authorization, confirmation, and audit boundaries are unchanged.
Deployment implications: Restart the main TALOS/text-agent process and voice worker so they load the new .env. Ollama must be running and mb-core-v1:latest must remain installed. The ignored .env is machine-local and is not included in source control.
Unresolved questions: None for the bounded streamed Chat Completions cutover. Local TTS and legacy Responses migration require separate owner authorization if full-stack offline operation is the next goal.
Current repository state: Bounded cutover implemented and verified; live `.env` recovered and merged additively; extensive unrelated pre-existing working-tree modifications preserved.
Next permitted task: Owner live voice verification, or a separately authorized local TTS/legacy Responses migration; do not execute physical actions during smoke testing without owner direction.
Required reading for next session: AGENTS.md, IMPLEMENTATION_STATUS.md, this handoff, talos/agent/runtime.py, talos/voice/agent.py, talos/voice/backends/factory.py, tests/test_llm_openai_compat.py.
Explicit stop point: Stop after local Ollama/STT boundary configuration, focused regression and harmless loopback validation, status, and handoff. Do not begin local TTS or legacy Responses migration automatically.
```
