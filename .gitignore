.env
.env.*
!.env.example

# Local Python environments and caches
.venv/
.venv-*/
venv/
__pycache__/
*.py[cod]

tasks.cpython-39.pyc
tv_control.cpython-39.pyc

# macOS / editor metadata
.DS_Store
.vscode/
.Rhistory

# Generated local assets
*_wirecache.npz
db/*.sqlite
db/*.sqlite3
db/*.sqlite3-*
# Runtime sleep-mode flag: machine state, not configuration.
db/talos_sleep_state.json

# Project-specific ignores
tests/persistent_convo.py

# Launcher: per-machine component/GPU choices (settings live in settings.env)
launcher.config.json

# Runtime log output: telemetry, benchmarks, and exact LLM transcripts (which
# contain prompts, conversation, context, and tool data). All machine output,
# never source.
logs/
talos/logs/

# Local artifact/data directory for the awareness subsystem
db/awareness/
